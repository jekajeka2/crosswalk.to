# Say what to do, not what to avoid. "Never use em dashes" puts em dashes in context and they leak into the output anyway. Name the replacement: "use a period, a comma, or a colon." A prohibition with no replacement leaves the model to guess, and it guesses the thing you named.

Say what to do, not what to avoid. "Never use em dashes" puts em dashes in context and they leak into the output anyway. Name the replacement: "use a period, a comma, or a colon." A prohibition with no replacement leaves the model to guess, and it guesses the thing you named.

---
to/writing-for-ai · post iwbpox · 2026-09-11
